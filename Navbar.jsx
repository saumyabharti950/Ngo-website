import { useState } from "react";
import { Menu, X, ArrowRight } from "lucide-react";
const links = [["home", "Home"], ["about", "About"], ["work", "Our Work"], ["gallary", "Gallery"],["language", "Language"], ["approach", "Approach"], ["partnerships", "Partnerships"], ["research", "Research"], ["contact", "Contact"]];
function Navbar() { const [mobileOpen, setMobileOpen] = useState(false); const jump = id => { document.getElementById(id)?.scrollIntoView({ behavior: "smooth" }); setMobileOpen(false); }; return <header className="navbar"><div className="container navbar-container"><button className="logo" onClick={() => jump("home")} aria-label="SIFI Foundation home"><div className="logo-mark">S</div><div className="logo-text"><strong>SIFI</strong><small>FOUNDATION</small></div></button><nav className="desktop-nav">{links.map(([id, label]) => <button key={id} onClick={() => jump(id)}>{label}</button>)}</nav><div className="nav-actions"><button className="btn btn-primary nav-donate" onClick={() => jump("partnerships")}>Partner With Us <ArrowRight size={17}/></button><button className="mobile-menu" onClick={() => setMobileOpen(!mobileOpen)} aria-label="Toggle navigation">{mobileOpen ? <X size={25}/> : <Menu size={25}/>}</button></div></div>{mobileOpen && <nav className="mobile-nav">{links.map(([id, label]) => <button key={id} onClick={() => jump(id)}>{label}</button>)}<button className="mobile-donate" onClick={() => jump("partnerships")}>Partner With Us</button></nav>}</header>; }
export default Navbar;

